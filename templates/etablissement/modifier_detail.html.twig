{% extends 'Layouts/base.html.twig'%}
{% block title 'Modifier L\'etablissement'~ etablissement.getId() %}
{% block body %}

    <div class="row">
        <div class="mt-3 mb-3 col-md-6 mx-auto">
            <h1>Modifier L'etablissement {{etablissement.getNomEtablissement()}}</h1>
            {{ include('etablissement/_form.html.twig',{submitButtonText:'Modifier l\'etablissement'}) }}
        </div>
    </div>
{% endblock %}
